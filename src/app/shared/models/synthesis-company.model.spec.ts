import { SynthesisCompany } from './synthesis-company.model';

describe('SynthesisCompany', () => {
  it('should create an instance', () => {
    expect(new SynthesisCompany()).toBeTruthy();
  });
});
